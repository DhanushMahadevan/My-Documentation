IN COMPANY , EVERY EMPLOYEE USES IAM USES and Linux user too. RARELY A ADMIN USERS THE ROOT USER ACCOUNT

USE AWS CONFIGURE ON EACH LINUX USER HOME DIR.


{NOTE: IF USERS USE GIT, THEY WON'T EXECUTED --global ,So executed only git config --global user.ema}
CREATE GROUPS LIKE DEV,TEST,CLOUD-ENG,ADMIN

1. ADMIN -ADMIN ACCESS OR  FULL ACCESS.

2. DEV - MIGHT GIVE ACCESS OF CREATE INSTANCE OR ROLE FOR S3-EC2 TO PUSH CODE TO S3 OR role for code deploy.

3. TEST - READONLY ACCESS.

4. CLOUDENG - WE CREATE INFRA, SO WE CONFIGURED UNDER 80% ACCESS.   